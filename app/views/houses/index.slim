h1 Visible
h2.text-center House list
table.table
  thead class="thead-dark"
    th Number
    th Address
    th Route
    th Status
    th{colspan=3}
  tbody
  - @houses.each do |house|
    tr
      td =house.number
      td =house.address
      td =house.route
      td =house.status
      td =link_to "Details", house_path(house), class: "btn btn-dark btn-sm"
      td =link_to "Edit", edit_house_path(house), class: "btn btn-dark btn-sm"
      td =link_to "Delete", house_path(house), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-dark btn-sm"
